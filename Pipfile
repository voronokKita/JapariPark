# This is an umbrella
# for development of the whole project

[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"


[packages]
# Misc
requests = "*"

# Frontend
Flask = ">=2.3.0,<3.0.0"
gunicorn = ">=20.1.0,<21.0.0"

# Backend
Django = ">=4.0.0,<5.0.0"
daphne = ">=4.0.0,<5.0.0"

# Testing
# !do not run `pytest .` from the base repository dir
# because it is an umbrella for other projects
# and will throw import errors
pytest = "*"
Werkzeug = "*"

# Linter
wemake-python-styleguide = "*"

# Docs
Sphinx = "*"
autodoc = "*"
myst-parser = "*"


[dev-packages]

[requires]
python_version = ">=3.10.0,<3.12.0"
